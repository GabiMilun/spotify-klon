# Backend docker setup

- run `docker compose up --build` to start mysql, app, and phpmyadmin
- api: http://localhost:6970
- frontend: http://localhost:5175
- phpmyadmin: http://localhost:8080 (user `gabo`, pass `milkakeks`)
